ordering в классе мета - показывает то в каком порядке будут упорядочены те или иные функции (теги, классы и тд.)
display_tags - gреобразует список тегов в строку через запятую
content_preview - предпросмотр текста, в моем случае обрезает текст комментария до 50 символов и заканчивает их '...' return obj.content[:50] + '...' if len(obj.content) > 50 else obj.content
path('login/', auth_views.LoginView.as_view(template_name='news/login.html'), name='login'), - данная строка отвечает за кастомный логин пользователя, прикрепленный к шаблону news/login.html
auth_views: Импорт из django.contrib.auth.views
LoginView: Встроенный класс-представление для аутентификации
as_view(): Метод, который преобразует класс-based view в функцию для URL
AJAX в комментариях - не нужно обновлять страницу при добавлении комментария, список комментариев обновляется сразу, пользователь сразу видит свой комментарий, без AJAX в комментариях страница бы перезагружалась и медленная работа.
if request.headers.get('X-Requested-With') == 'XMLHttpRequest': - проверяет, пришел ли запрос от JavaScript (AJAX) или обычный переход по ссылке.
article.comments_enabled: - проверяет, включены ли комментарии к статье или нет
with - используется чтобы недопускать повторных вызовов db, делает код чище и понятнее first_block.image.url вместо article.blocks.first.image.url, обрабатывает первый блок статьи в зависимости от типа и отображает соответствующее превью 